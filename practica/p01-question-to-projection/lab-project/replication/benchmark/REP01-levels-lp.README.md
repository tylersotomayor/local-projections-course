# REP01: levels-lp

Produced by `Example1_LongDifferences/SSBias_IntcpYLagdiffN_95.do` lines 83–89 (levels regressions), 125–128 and 139–140 (stored draws and MC mean), through `all-simulate.do` lines 18–44; only driver line 19 changes 10000 to 500. Figure driver `all-figures.do` lines 16–37 plots the 0.95 panel.

N=99−h is calculated from the complete 100-observation simulated sample and one lag plus h leads. It is not a captured Stata e(N). Regression standard errors and full-draw MC standard errors cannot be recovered from the author output; fields are blank.
